# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace files
go.work
go.work.sum

# Dependency directories (uncomment if you want to ignore vendored dependencies)
#vendor/

# Environment variables and secrets (critical for security - never commit these)
.env
.env.local
.env.test
.env.production
*.secret
*.key
*.pem  # e.g., SSL certificates or private keys

# Logs and temporary files
*.log
logs/
tmp/
temp/

# Database dumps or local files (if using SQLite or similar for dev)
*.db
*.sqlite
*.sql.dump

# IDE/editor configurations (VS Code specific)
.vscode/
.idea/

# Build artifacts and caches
/bin/
/pkg/
/debug/
/coverage/

# OS-specific files
.DS_Store
Thumbs.db

# Other common ignores for APIs
node_modules/  # If any JS tools are mixed in
dist/
build/